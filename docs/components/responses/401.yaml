description: 'Not Authorized'
content:
  application/json:
    schema:
      type: object
      required:
        - error
        - message
      properties:
        error:
          type: string
          example: ForbiddenError
        message:
          type: string
          example: Not authorized
